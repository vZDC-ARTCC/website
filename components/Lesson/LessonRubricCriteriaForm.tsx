'use client';
import React from 'react';
import {LessonRubricCriteria} from "@prisma/client";

export default function LessonRubricCriteriaForm({rubricCriteria}: { rubricCriteria: LessonRubricCriteria }) {

    return (
        <div></div>
    );
}